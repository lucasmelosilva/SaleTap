import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    bundle: {
    flexGrow: 4/6,
    justifyContent: 'center',
    alignItems: 'center',
  },

  bundleImage: {
    flexDirection: 'row'
  },
  
  title: {
    fontSize: 18,
    color: '#F1F1F1',
    fontWeight: 'bold',
  },
})