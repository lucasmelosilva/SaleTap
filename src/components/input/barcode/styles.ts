import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
   content: {
    gap: 20
  },
  contentLabel: {
    fontSize: 15,
    fontWeight: 'semibold'
  },
  
  input: {
    padding: 15,
    fontSize: 18,
    backgroundColor: '#F1F1F1',
    color: '#000',
    borderRadius: 10,
  },
})